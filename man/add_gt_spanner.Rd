% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_gt.R
\name{add_gt_spanner}
\alias{add_gt_spanner}
\title{Add a spanner to a gt table}
\usage{
add_gt_spanner(
  gt_table,
  spanner_name,
  selection_type,
  columns,
  add_empty = TRUE
)
}
\arguments{
\item{gt_table}{A \code{gt} table object.}

\item{spanner_name}{Character string giving the label for the spanner.}

\item{selection_type}{Character string defining the selection method passed to \code{tidyselect_gt_column_name()} (e.g., \code{"contains"}, \code{"starts_with"}).}

\item{columns}{Column selection pattern to identify which columns to span.}

\item{add_empty}{Logical; if \code{TRUE}, adds an empty column before the first spanned column. Defaults to \code{TRUE}.}
}
\value{
A modified \code{gt} table with a spanner header (and optional empty column) added.
}
\description{
Adds a spanner header over selected columns in a \code{gt} table. Optionally, an empty column can be inserted before the first selected column.
}
\examples{
\dontrun{
gt_tbl <- gt::gt(head(mtcars))
add_gt_spanner(gt_tbl, "Performance", selection_type = "contains", columns = "hp")
}
}
